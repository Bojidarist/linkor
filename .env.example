# Required: Secret key for accessing the admin panel and API
ADMIN_SECRET_KEY=change-me-to-a-strong-secret

# Optional: HTTP server port (default: 8080)
PORT=8080

# Optional: Path to the SQLite database file (default: linkor.db)
DATABASE_PATH=linkor.db
